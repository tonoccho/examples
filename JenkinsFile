pipeline{
    agent any

    stages{
        stage("preparation"){
            steps {
                checkout scm
                sh "ls"
            }
        }
        stage("compilation") {
            steps{
                sh "./mvnw 'clean install'"
            }
        }
    }
    post{
        always{
            echo "========always========"
        }
        success{
            echo "========pipeline executed successfully ========"
        }
        failure{
            echo "========pipeline execution failed========"
        }
    }
}